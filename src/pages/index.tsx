import Layout from '@/components/layout/Layout';
import Meta from '@/components/layout/Meta';
import { basePageMeta } from '@/utils/metaConfigs';

const Index = () => <Layout meta={<Meta {...basePageMeta} />}>AAAA</Layout>;

export default Index;
